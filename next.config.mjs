/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['random.imagecdn.app','fastly.picsum.photos','picsum.photos','cdn.jsdelivr.net','ucarecdn.com'],
      },
};

export default nextConfig;
